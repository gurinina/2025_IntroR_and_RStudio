% Basic packages for LaTeX
\usepackage{graphicx}    % For including images
\usepackage{amsmath}     % For advanced math typesetting
\usepackage{hyperref}    % For creating hyperlinks
\usepackage{fancyvrb}    % For verbatim environments and code
\usepackage{longtable}   % For tables that span multiple pages
\usepackage{booktabs}    % For better table styling

% Disable List of Figures and List of Tables
\renewcommand{\listoffigures}{}
\renewcommand{\listoftables}{}

% Custom formatting
\setlength{\parindent}{0pt}             % Remove paragraph indentation
\setlength{\parskip}{1em}               % Add space between paragraphs

% Page geometry (remove this if you handle margins in output.yml)
\usepackage[top=1in, left=0.5in, right=0.5in, bottom=0.5in]{geometry}

% Custom commands
\newcommand{\vect}[1]{\boldsymbol{#1}}  % Command to bold vectors

% PDF-specific configurations
\hypersetup{
  colorlinks=true,                       % Enable colored links in the PDF
  linkcolor=blue,                        % Color of internal links
  urlcolor=cyan                          % Color of URLs
}
